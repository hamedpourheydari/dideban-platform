# Dideban UI Phase 9

- پنل یادداشت با Grid Area صریح به سمت راست منتقل شد.
- جهت RTL متن‌ها حفظ شد و فقط ترتیب فیزیکی ستون‌ها مستقل از RTL گردید.
- تا چهار دوربین ثبت‌شده با همان روال اصلی `monitor="watch"` شینوبی به‌صورت خودکار باز می‌شوند.
- اگر فقط یک دوربین در تنظیمات `watch_on` ذخیره شده باشد، دوربین دوم دیگر با Placeholder جایگزین نمی‌شود.
- Backend، FFmpeg، Socket، ضبط و دیتابیس تغییری نکرده‌اند.
